<UserControl x:Class="MedicineCareBridge.Client.Views.Auth.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MedicineCareBridge.Client.Views.Auth"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Background="#F0F0F0">
        <Border CornerRadius="10"
                Background="White"
                Padding="20"
                Margin="30"
                BorderBrush="#DDDDDD"
                BorderThickness="1"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                
                <StackPanel Grid.Column="0"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Stretch"
                            
                            Margin="0,0,20,0">
                    <TextBlock Text="Авторизация"
                               FontSize="22"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,10"/>

                    <TextBlock Text="Логин" FontWeight="SemiBold"/>
                    <TextBox x:Name="UsernameTextBox"
                             Height="30"
                             Margin="0,0,0,5"/>

                    <TextBlock Text="Пароль" FontWeight="SemiBold"/>
                    <PasswordBox x:Name="PasswordBox"
                                 Height="30"
                                 Margin="0,0,0,10"/>

                    <Button x:Name="LoginButton"
                            Content="Войти"
                            Height="35"
                            Background="#4CAF50"
                            Foreground="White"
                            FontWeight="Bold"
                            Click="LoginButton_Click"/>

                    <TextBlock x:Name="ErrorTextBlock"
                               Text="Ошибка входа: неверный логин или пароль"
                               Foreground="Red"
                               Visibility="Collapsed"
                               TextWrapping="Wrap"
                               Margin="0,10,0,0"/>
                </StackPanel>

                
                <Border Grid.Column="1"
                        Background="#F9F9F9"
                        CornerRadius="6"
                        Padding="10"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Width="160"
                        Height="160">
                    
                <Image x:Name="LogoImage"
                           Stretch="Uniform"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Source="/Resources/img/logo.png"/>          


                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
