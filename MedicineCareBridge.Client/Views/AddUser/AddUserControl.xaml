<UserControl x:Class="MedicineCareBridge.Client.Views.AddUser.AddUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MedicineCareBridge.Client.Views.AddUser"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Фамилия:" />
            <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Имя:" Margin="0,8,0,0" />
            <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Отчество:" Margin="0,8,0,0" />
            <TextBox Text="{Binding MiddleName, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Логин:" Margin="0,8,0,0" />
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Пароль:" Margin="0,8,0,0" />
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" />

            <TextBlock Text="Роль:" Margin="0,8,0,0" />
            <ComboBox ItemsSource="{Binding Roles}" SelectedItem="{Binding SelectedRole}" />

            <TextBlock Text="Права:" Margin="0,8,0,0" />
            <StackPanel Orientation="Horizontal" >
                <CheckBox Content="Чтение" IsChecked="{Binding CanRead}" Margin="0,0,10,0"/>
                <CheckBox Content="Запись" IsChecked="{Binding CanWrite}" Margin="0,0,10,0"/>
                <CheckBox Content="Удаление" IsChecked="{Binding CanDelete}" />
            </StackPanel>
        </StackPanel>

        <Button Content="Заполнить документы" Grid.Row="2" HorizontalAlignment="Right" Width="150"
                Margin="0,10,0,0"
                Command="{Binding FillDocumentsCommand}" />
    </Grid>
</UserControl>
